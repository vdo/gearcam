<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="studioBackground">#0C1219</item>
        <item name="studioPanel">#17222D</item>
        <item name="studioBorder">#2A3B49</item>
        <item name="studioText">#EDF4FA</item>
        <item name="studioMuted">#A5B8C8</item>
        <item name="studioAccent">#72E5C0</item>
        <item name="android:imageButtonStyle">@style/image_button</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="colorAccent">?attr/studioAccent</item>
        <item name="android:colorAccent">?attr/studioAccent</item>
        <item name="android:colorBackground">?attr/studioBackground</item>
        <item name="android:textColorPrimary">?attr/studioText</item>
        <item name="android:textColorSecondary">?attr/studioMuted</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:alertDialogTheme">@style/StudioAlertDialog</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>
    </style>

    <style name="image_button" parent="@android:style/Widget.Material.ImageButton">
        <item name="android:backgroundTint">@null</item>
    </style>
    <style name="StudioAlertDialog" parent="android:style/Theme.Material.Dialog.Alert">
        <item name="android:colorAccent">?attr/studioAccent</item>
        <item name="android:colorBackground">?attr/studioBackground</item>
        <item name="android:textColorPrimary">?attr/studioText</item>
        <item name="android:textColorSecondary">?attr/studioMuted</item>
        <item name="android:windowBackground">@drawable/studio_card</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
</resources>
